<div class="grid_6">
  <ul class="minigallery">
  <% Gallery.first.photos.each do |photo| %>
    <li><%= link_to(image_tag(photo.image.url(:thumb), :class => "faded", :alt => photo.caption), photo.image.url,  :rel => "prettyPhoto[homegallery]") %></li>
  <% end %>
  </ul>
</div>
